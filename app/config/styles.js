import { StyleSheet } from 'react-native';
import { COLOR } from 'react-native-material-ui';

const MaterialDesignTheme = {
  palette: {
    primaryColor: COLOR.amber500,
  },
};

const Styles = StyleSheet.create({});

export { MaterialDesignTheme, Styles };
